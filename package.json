{
    "private": true,
    "version": "1.0.0",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "serve:client": "yarn --cwd ./packages/client serve",
        "build:client": "yarn --cwd ./packages/client build",
        "test:client": "yarn --cwd ./packages/client test"
    },
    "postcss": {
      "plugins": {}
    },
    "browserslist": [
      "> 1%",
      "last 2 versions"
    ]
}
